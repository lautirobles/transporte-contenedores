# spring.application.name=logistica


# # ==============================
# # CONFIGURACIÓN SERVIDOR
# # ==============================
# server.port=8082

# # ==============================
# # BASE DE DATOS H2 PERSISTENTE (en archivo)
# # ==============================
# # Guardará el archivo dentro de la carpeta /data de tu proyecto
# spring.datasource.url=jdbc:h2:file:./data/logisticadb
# spring.datasource.driverClassName=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=sa
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# # No borrar los datos al reiniciar
# # Opciones: create, create-drop, update, validate, none
# spring.jpa.hibernate.ddl-auto=update

# # ==============================
# # CONSOLA H2 (para inspeccionar la BD)
# # ==============================
# spring.h2.console.enabled=true
# spring.h2.console.path=/h2-console

# # ==============================
# # LOGGING SQL (opcional)
# # ==============================
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.format_sql=true
# spring.jpa.defer-datasource-initialization=true
# # Ejecuta data.sql si existe
# spring.sql.init.mode=embedded

# # =================================================
# # CONFIGURACIÓN DE KEYCLOAK (RESOURCE SERVER) Y JWT
# # =================================================
# spring.security.oauth2.resourceserver.jwt.issuer-uri=http://keycloak:8080/realms/tpi-backend

# keycloak.realm=logistica-realm
# keycloak.resource=backend-logistica  # ← EXACTAMENTE igual
# keycloak.auth-server-url=http://localhost:8080/auth
# keycloak.public-client=true


# # OAuth2 Configuration
# spring.security.oauth2.client.registration.keycloak.client-id=transport-app
# spring.security.oauth2.client.registration.keycloak.client-secret=0gvrzFXPl9rrU7w3vSlmpVKXqZAv1Cfj
# spring.security.oauth2.client.registration.keycloak.scope=openid,profile,email
# spring.security.oauth2.client.registration.keycloak.authorization-grant-type=authorization_code
# spring.security.oauth2.client.registration.keycloak.redirect-uri=http://localhost:8080/login/oauth2/code/keycloak

# spring.security.oauth2.client.provider.keycloak.issuer-uri=http://localhost:8080/auth/realms/tpi-backend



spring.application.name=logistica
server.port=8082

# Base de datos
spring.datasource.url=jdbc:h2:file:./data/logisticadb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=sa
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update

# SOLO esto para OAuth2 (Resource Server)
spring.security.oauth2.resourceserver.jwt.issuer-uri=http://localhost:8080/realms/tpi-backend

# ELIMINÁ todo lo demás de keycloak.* y oauth2.client.*